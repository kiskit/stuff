# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page: movies list
GET     /                           controllers.Application.index(filter ?= None)
GET     /admin                      controllers.Application.admin()
GET		/login						controllers.Application.login()
POST	/login						controllers.Application.authenticate()
GET     /userlist                   controllers.Application.userlist(filter ?= None)
GET     /useredit                   controllers.Application.useredit(id ?= None)
GET     /movieedit                  controllers.Application.movieedit(id ?= None)
GET     /sessionwarning             controllers.Application.sessionWarning()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

