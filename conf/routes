# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~


GET 	/admin						controllers.Application.index()
POST 	/admin						controllers.Application.admin()
GET     /login                  	controllers.Application.login()
POST    /login                  	controllers.Application.authenticate()
GET     /logout                  	controllers.Application.logout()


# Home page for all users
#GET     /                           controllers.Videos.index()

# Where the menu for the admin is located
#GET 	/admin						controllers.Application.admin()

# Where the history is located
#GET 	/history					controllers.Application.history()
GET		/borrow						controllers.Videos.borrow()
POST	/borrow						controllers.Videos.validateBorrow()

# Video edition page
GET		/videos						controllers.Videos.index()
GET		/videoedit/:id				controllers.Videos.editVideo(id: Long)
POST	/videoedit					controllers.Videos.validateVideo()
GET		/videoinfo/:id				controllers.Videos.videoInfo(id: Long)
#POST	/videoedit/:id				controllers.Videos.validateVideoInfo(id: Long)




# Ajax methods
GET		/rentals					controllers.Videos.getUserRentals(userId: Long ?= 0, videoId: Long ?= 0)
GET		/videolist					controllers.Videos.getVideoList(pageNumber: Integer, pageSize: Integer)
GET		/videobytitleorid			controllers.Videos.getVideoByTitleOrId(titleOrId: String)
GET		/tmdb						controllers.Videos.getTMDBTitles(title: String)
# End Ajax methods



GET		/doCheckin					controllers.Videos.doCheckin(list: String)
GET		/doCheckout					controllers.Videos.doCheckout(userId: Long, list: String)

# Video edition page
GET		/users						controllers.Users.index()
GET		/useredit/:id				controllers.Users.editUser(id: Long)
POST	/useredit					controllers.Users.validateUser()


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
